import config.IrkConfig
import http.RequestContainer

object Runner{
    
    def main(args: Array[String]): Unit = {
        // load configuration from file
     //   if(!IrkConfig.init(args)) System.exit(2)
    
        //load entities
      //  if(IrkConfig.conf.entitiesPath.isDefined)
        //val entityContainer = new EntityContainer(IrkConfig.conf.entitiesPath)
        //if(entityContainer.isEmpty) System.exit(2)
    }
    
   
    
}
